 #keyword arguments are the arguments that are passed to a function by explicitly specifying the parameter name and its corresponding value.
#the order of the arguments does not matter when using keyword arguments
def greet(name, age):
    print(f"Hello, {name}! You are {age} years old.")
greet(age=30, name="Alice")

